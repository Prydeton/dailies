import { styled } from 'goober'

export const Container = styled('header')`
  background: var(--grey);
  height: 80px;
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;

  h2 { 
    margin: 0;
  }
`
