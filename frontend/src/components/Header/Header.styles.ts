import { styled } from 'goober'

export const Container = styled('header')`
  background: var(--grey);
  height: 60px;
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;

  h1 { 
    margin: 0;
  }
`
